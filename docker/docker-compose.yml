version: '3.8'

services:
  api:
    build: .
    image: python-web-api:latest
    container_name: python-web-api
    network_mode: host
    environment:
      - FLASK_APP=app.py
      - PYTHONUNBUFFERED=1
    restart: unless-stopped
