version: '3.8'

services:
  api:
    build: .
    image: python-web-api:latest
    container_name: python-web-api
    ports:
      - "5000:5000"
    environment:
      - FLASK_APP=app.py
      - PYTHONUNBUFFERED=1
    restart: unless-stopped
